apply plugin: "war"

bootWar {
    mainClassName = "com.akurat.sms.SampleMicroServicesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
